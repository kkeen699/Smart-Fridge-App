{
  "name": "smart-fridge-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start-server": "cd server && npm run dev",
    "start-client": "cd client && npm start",
    "start-firebase": "firebase emulators:start --import=./firebase-data --export-on-exit",
    "dev": "concurrently \"npm run start-server\" \"npm run start-client\" \"npm run start-firebase\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
